1. Create a chain including RxPdu with at most one parameter each (1000)
2. Create a chain including RxPdu with at most 5 parameters each (2000)
3. Create with only ChannelConfig with at most one parameter (500 only)
4. Create with only ChannelConfig with at most 5 parameters (500 only)
5. Create a chain including TxPdu (which is one) with at most one parameter (500 only)
6. Create a chain including UserDataTxPdu with at most one parameter (500)
8. Create multiple (at most 4) sets of RxPdu inside ChannelConfig, at most one parameter for ChannelConfig but exactly one for RxPdu (1000)
9. Create multiple (at most 4) sets of ChannelConfig with exactly one parameter (1000)
10. Create PnInfo with at most one parameter but with multiple sets (at most 4) of CanNmPnFilterMaskByte with exactly one parameter (1000)
11. Create wiht only a single ChannelConfig subcontainer with at most 5 parameters (or one parameter)
12. Create multiple (at most 4) sets of RxPdu inside multiple sets of ChannelConfig, at most two parameter for ChannelConfig but 5 for RxPdu (1000)
